//CCC '01 S1 - Keeping Score
#include <iostream>
#include <vector>

using namespace std;

char c[4][13];

int main() {
    std::cout << "Hello, World!" << std::endl;
    string s;
    cin >> s;

    char c[4][13];
   // c[0] = s.substr(s.find('C'),s.find('D') - s.find('C')).c_str()
    return 0;
}

void addToArray(int x, char* y[])
{
    for(int
}

